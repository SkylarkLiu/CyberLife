import { ModulePlaceholder } from "@/components/module-placeholder";

export default function BaguaPage() {
  return (
    <ModulePlaceholder
      title="八卦"
      subtitle="占位模块"
      description="八卦模块目前作为结构占位存在，用于保证首页导航和后续扩展路线完整。当前不实现独立算法与结果流程。"
    />
  );
}
