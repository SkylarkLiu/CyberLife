import { ModulePlaceholder } from "@/components/module-placeholder";

export default function DaLiuRenPage() {
  return (
    <ModulePlaceholder
      title="大六壬"
      subtitle="占位模块"
      description="大六壬将保留在统一架构中逐步接入。当前页面只提供视觉和路由占位，暂不进入术数引擎实现阶段。"
    />
  );
}
